import { EmptyState } from '@/components/signals/empty-state';

export default function DemoTradesPage() {
  return (
    <EmptyState
      title="Trade Tracker (Phase 11)"
      description="Track your trades, positions, and P&L — coming in Phase 11."
    />
  );
}
